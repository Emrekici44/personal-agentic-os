import {RuntimePolicyError} from "../policies/evaluator.ts";import type {ActionOrigin,ConnectorAction} from "./types.ts";
export const connectorLimits=Object.freeze({maxReadTools:8,maxMutationProposals:3,maxDestructiveProposals:1,maxTargetsPerDestructiveAction:1});
export function approvalClass(connector:"google_calendar"|"google_tasks"|"obsidian",action:Exclude<ConnectorAction,"read">){return`${connector}_${action}`}
export function assertExternalActionPolicy(input:{action:ConnectorAction;origin:ActionOrigin;targetIds:string[];approved:boolean}){if(input.action==="read")return;if(input.targetIds.length!==1)throw new RuntimePolicyError("bulk_external_action_denied","Externe Mutationen müssen genau ein Ziel adressieren");if(input.action==="delete"&&!input.approved)throw new RuntimePolicyError("destructive_approval_required","Löschen braucht immer eine explizite Action-Time-Freigabe");if((input.origin==="agent_proposal"||input.origin==="scheduled")&&!input.approved)throw new RuntimePolicyError("approval_required","Agentische und geplante externe Mutationen benötigen Freigabe");if(input.origin==="scheduled"&&input.action==="delete")throw new RuntimePolicyError("scheduled_delete_denied","Scheduler darf niemals löschen")}
