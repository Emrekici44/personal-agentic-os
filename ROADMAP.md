# Roadmap

## Expanded end-state checkpoint (complete locally)

- Complete multi-domain UI and mobile-first navigation: implemented and browser-verified
- Original dark futuristic command-center redesign: implemented; left desktop navigation preserved
- User identity audit: `Emre` verified in visible defaults; mistaken names removed
- Editable temporary product identity and transparent cost consent: implemented
- Three provider modes plus Usage & Limits and pre-request kill switch: implemented, API remains disabled
- Emre vault additive system layer: implemented; original notes preserved
- Graphify: optional pilot only; built-in free graph remains core
- Local end-state verified: tests, production build, documentation, VS Code handoff, and desktop/mobile previews complete
- Private source ownership: published to `Emrekici44/personal-agentic-os` with `main` as the default branch

## Phase 1 — Foundation (complete)

- Responsive desktop/mobile shell and all nine core modules
- Local shared state, idea capture, approvals, audit, backup
- Calm design system and representative user-specific routines

## Phase 2 — Agent workflows (complete)

- Weekly planner: maximum three outcomes, 30–40% buffer, energy constraints, approval gate
- Project, faith/reflection, health, finance, and relationship workflows
- Clear non-professional boundaries for health and finance

## Phase 3 — Durable brain (operational local adapter)

- Knowledge graph surface, shared audit history, local persistence
- Obsidian-compatible safe import contract: read-only preview before any indexing
- Existing vault content preserved; authorized additive Emre system structure complete

## Phase 4 — Google Calendar connector (implementation complete; consent pending)

- Secure OAuth callback, encrypted HTTP-only token boundary, status and calendar selection
- Real reads limited to eight days; `calendar.readonly` scope only
- Proposal-only focus blocks and exact approval contract; mock proves zero writes
- Account-side OAuth client creation and user consent remain pending
- Tasks, health, and finance adapters remain opt-in and read-only by default

## Phase 5 — Private mobile deployment (requires user approval)

- Select a private hosting/account boundary
- Configure secrets in host, enable authentication, verify backup/restore
- Never publish publicly or create a paid service without approval
