import {NextRequest,NextResponse} from 'next/server';import {calendarConfig,calendarScopes,hasStoredToken,refreshedAccessToken,tokenScopes} from '@/lib/google-calendar';import {verifyLocalSession} from '@/lib/shared-store';
const headers={'Cache-Control':'no-store, private'};
export async function GET(req:NextRequest){if(!verifyLocalSession(req.cookies.get('agentic_os_local_session')?.value))return NextResponse.json({error:'Lokale Sitzung erforderlich',credentialsExposed:false},{status:401,headers});const cookie=req.cookies.get('agentic_os_google_token')?.value,c=calendarConfig(),granted=tokenScopes(cookie),connected=Boolean(await refreshedAccessToken(cookie)),eventWriteReady=connected&&calendarScopes.every(scope=>granted.includes(scope));return NextResponse.json({configured:c.configured,connected,eventWriteReady,sharedWithDesktop:hasStoredToken(),mode:connected?'google':c.configured?'oauth-ready':'unconfigured',permissions:calendarScopes.map(scope=>scope.split('/').pop()),writesEnabled:eventWriteReady,writesRequireExactApproval:true,backgroundWrites:false,deletesEnabled:false,credentialsExposed:false},{headers})}
