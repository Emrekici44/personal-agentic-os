import crypto from 'node:crypto';import {NextResponse} from 'next/server';import {oauthUrl,seal} from '@/lib/google-calendar';
export async function GET(){try{const state=crypto.randomBytes(24).toString('base64url'),r=NextResponse.redirect(oauthUrl(state));r.cookies.set('agentic_os_oauth_state',seal(state),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:600,path:'/'});return r}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'OAuth nicht verfügbar'},{status:503})}}
