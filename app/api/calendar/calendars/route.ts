import {NextRequest,NextResponse} from 'next/server';import {refreshedAccessToken} from '@/lib/google-calendar';import {verifyLocalSession} from '@/lib/shared-store';
const headers={'Cache-Control':'no-store, private'};
export async function GET(req:NextRequest){if(!verifyLocalSession(req.cookies.get('agentic_os_local_session')?.value))return NextResponse.json({error:'Lokale Sitzung erforderlich',calendars:[]},{status:401,headers});const token=await refreshedAccessToken(req.cookies.get('agentic_os_google_token')?.value);if(!token)return NextResponse.json({mode:'unavailable',connected:false,label:'Keine verifizierte Google-Verbindung',calendars:[],mockDataUsed:false},{status:409,headers});const r=await fetch('https://www.googleapis.com/calendar/v3/users/me/calendarList?minAccessRole=reader',{headers:{authorization:`Bearer ${token}`},cache:'no-store'});if(!r.ok)return NextResponse.json({error:'Google Calendar konnte nicht gelesen werden',calendars:[]},{status:502,headers});const d=await r.json();return NextResponse.json({mode:'google',connected:true,label:'Google Calendar · Rollen verifiziert',calendars:(Array.isArray(d.items)?d.items:[]).map((x:any)=>({id:x.id,summary:x.summary,primary:Boolean(x.primary),selected:x.selected!==false,accessRole:x.accessRole,writable:['writer','owner'].includes(x.accessRole)})),mockDataUsed:false},{headers})}
