{
  "name": "agentic-os-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Local secure desktop shell for Agentic OS",
  "main": "main.cjs",
  "productName": "Agentic OS",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.cjs && node --check preload.cjs && node --test test/*.test.cjs"
  },
  "devDependencies": {
    "electron": "43.3.0"
  }
}
