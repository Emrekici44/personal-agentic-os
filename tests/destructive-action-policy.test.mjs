import assert from "node:assert/strict";import test from "node:test";import {assertExternalActionPolicy,connectorLimits} from "../lib/runtime/connectors/policy.ts";import {connectorCapabilityMatrix} from "../lib/runtime/connectors/capability-matrix.ts";
test("destructive external actions are exact, single-target, action-time approved and never scheduled",()=>{assert.throws(()=>assertExternalActionPolicy({action:"delete",origin:"agent_proposal",targetIds:["one"],approved:false}),/Action-Time/);assert.throws(()=>assertExternalActionPolicy({action:"delete",origin:"scheduled",targetIds:["one"],approved:true}),/Scheduler/);assert.throws(()=>assertExternalActionPolicy({action:"delete",origin:"user_direct",targetIds:["one","two"],approved:true}),/genau ein Ziel/);assert.doesNotThrow(()=>assertExternalActionPolicy({action:"delete",origin:"user_direct",targetIds:["one"],approved:true}));assert.equal(connectorLimits.maxDestructiveProposals,1)});
test("capability matrix never upgrades missing scopes",()=>{const matrix=connectorCapabilityMatrix();for(const capability of matrix.filter(item=>item.connector==="google_tasks"))assert.equal(capability.status,"scope_missing");assert.equal(matrix.length,12)});
