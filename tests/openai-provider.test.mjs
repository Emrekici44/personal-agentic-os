import test from'node:test';import assert from'node:assert/strict';import{providerPolicy,assertRequestAllowed}from'../lib/openai-provider.mjs';test('zero-cost policy is disabled with kill switch by default',()=>{const p=providerPolicy({});assert.equal(p.mode,'disabled');assert.equal(p.killSwitch,true);assert.throws(()=>assertRequestAllowed(p),/disabled/)});test('API request requires key, limits and disabled kill switch',()=>{const p=providerPolicy({OPENAI_MODE:'api',OPENAI_API_KEY:'server-only-test',OPENAI_KILL_SWITCH:'off',OPENAI_DAILY_LIMIT_EUR:'1',OPENAI_MONTHLY_LIMIT_EUR:'5'});assert.equal(assertRequestAllowed(p,{daily:0,monthly:0}),true);assert.throws(()=>assertRequestAllowed(p,{daily:1,monthly:0}),/ceiling/)})
