import assert from "node:assert/strict";import test from "node:test";import {nextDue} from "../lib/repositories/schedule-repository.ts";
test("scheduler computes bounded Europe Berlin daily and weekly due times",()=>{const daily=nextDue({cadence:"daily",hour:8,minute:0},new Date("2026-03-28T22:00:00Z"));assert.match(daily,/2026-03-29T06:00:00.000Z/);const weekly=nextDue({cadence:"weekly",dayOfWeek:0,hour:9,minute:0},new Date("2026-03-28T22:00:00Z"));assert.match(weekly,/2026-03-29T07:00:00.000Z/);assert.throws(()=>nextDue({cadence:"weekly",dayOfWeek:9,hour:9,minute:0}),/Wochentag/)});
test("scheduler API is manual, private and proposal-only",async()=>{const source=await import("node:fs/promises").then(fs=>fs.readFile(new URL("../app/api/runtime/schedules/route.ts",import.meta.url),"utf8"));for(const value of ["verifyLocalSession","trustedPrivateMutationOrigin","readPrivateJson","evaluate_due","backgroundExecution"])assert.match(source,new RegExp(value));assert.doesNotMatch(source,/setInterval|setTimeout|calendar.*write|OpenAI/i)});
